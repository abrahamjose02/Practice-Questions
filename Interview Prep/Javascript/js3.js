console.log("" > -1); //true ; Here the value is coerced to the same datatype and so "" will be 0 and 0>-1 true;

console.log("" == true); //false , Here type coersion happend and true is converted to 1 and '' is converted to 0 in number

console.log(typeof Nan); // Number ; It represents an invalid or undefined result of a numerical computation , but is still considered as a number datatype

